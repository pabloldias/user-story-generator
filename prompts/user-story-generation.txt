Generate a Jira user story from the extracted entities below.

The story_body MUST follow this exact template — do not deviate:
  "As a <actor>, I want <goal>, so that <value>."

Rules:
1. Use ONLY the information provided in the entities JSON.
2. Do NOT infer, assume, or add any detail not present in the input.
3. If an entity value is null, use the placeholder "[MISSING: <field_name>]" in the story body and add the field name to flags.
4. Choose the most appropriate priority and story_points based on the complexity implied by the goal.
5. labels must be lowercase, hyphen-separated strings (e.g. "user-auth", "payments").
6. confidence_score must be a rigorous, honest assessment (0.0 = unusable, 1.0 = perfect). Use the following calibration table — be strict:

   SCORE RANGE | CRITERIA
   0.00–0.15   | One or more entities are null; nothing actionable can be built.
   0.16–0.30   | All entities are present but the goal is so vague (e.g. "make it better",
               | "improve things", "fix issues") that no developer could implement it without
               | a clarifying conversation. The value/benefit is either missing or generic.
   0.31–0.50   | Entities are present but the goal lacks specificity (no measurable outcome,
               | no defined scope, uses hedging language like "more useful", "less confusing",
               | "right stuff", "easier", "nicer", "cleaner") and the value is implied rather
               | than explicitly stated.
   0.51–0.70   | Goal is reasonably specific but missing one concrete detail (e.g. scope,
               | acceptance condition, or a clear measurable outcome). Value is stated.
   0.71–0.85   | Goal is specific and actionable, value is stated and meaningful, actor is
               | correct. Minor ambiguity remains (e.g. edge cases unstated).
   0.86–1.00   | All three entities are explicit and unambiguous. The goal describes a
               | concrete, testable behaviour with named metrics, defined access rules, or
               | explicit technical constraints. The value is a clear business benefit. No
               | inference was required. A developer can implement this without clarification.

   Scoring guidance — read carefully before scoring:
   - A requirement delivered as an email or informal message is NOT automatically vague.
     Judge the content, not the format. If the message lists specific KPIs, named metrics,
     access-control rules, refresh cadence, or export formats, score it on content quality.
   - "For use in quarterly reviews" and similar purpose statements ARE valid business values.
     Do NOT penalise a value entity because it describes a use-case context — it is still
     an explicitly stated reason that justifies the feature.
   - Goals that enumerate specific, named items (e.g. four named KPIs with actual-vs-target
     comparisons, role-based visibility, daily data refresh, PDF export) satisfy the
     0.71–0.85 or 0.86–1.00 bands provided all three entities are present and unambiguous.

   Common traps that MUST lower the score into the 0.16–0.30 band:
   - Goal is essentially a complaint or a wish ("it should be better / more useful")
   - Goal contains only adjectives without a concrete action or measurable outcome
   - The source explicitly says "vague", "unclear", or uses phrases like "right stuff"

7. flags must contain ONLY codes from this exact list — do not invent new codes:
   VAGUE_GOAL          — goal uses non-specific language; no developer can implement without clarification
   NULL_ENTITY         — one or more of actor / goal / value is null
   INVALID_TEMPLATE    — story_body does not follow "As a … I want … so that …" format
   MISSING_FIELDS      — story_body contains [MISSING: …] placeholders
   LOW_CONFIDENCE      — confidence_score is below 0.50
   NO_MEASURABLE_OUTCOME — goal has no concrete, measurable outcome

   If none of these apply, return an empty array: "flags": []

8. Return valid JSON only — no markdown, no explanation outside the JSON object.

Entities:
{{entities}}

Respond with this exact JSON structure:
{
  "title": "<concise story title, max 80 chars>",
  "story_body": "As a <actor>, I want <goal>, so that <value>.",
  "priority": "Low" | "Medium" | "High" | "Critical",
  "story_points": 1 | 2 | 3 | 5 | 8 | 13,
  "labels": ["<label>"],
  "confidence_score": 0.0,
  "flags": ["<flag code if any>"]
}
