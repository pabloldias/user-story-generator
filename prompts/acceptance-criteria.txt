Generate acceptance criteria for the user story below using the Given / When / Then format.

Rules:
1. Generate between 2 and 5 criteria — no more, no fewer.
2. Each criterion must be specific and independently testable.
3. Derive criteria ONLY from information explicitly present in the story.
4. Do NOT invent edge cases, users, or system behaviours not implied by the story.
5. Each "given", "when", and "then" must be a single, concise sentence.
6. Return valid JSON only — no markdown, no explanation outside the JSON object.

User Story:
{{story}}

Respond with this exact JSON structure (an object wrapping the array so JSON-object mode is satisfied):
{
  "criteria": [
    {
      "given": "<initial context>",
      "when":  "<action taken>",
      "then":  "<expected outcome>"
    }
  ]
}
